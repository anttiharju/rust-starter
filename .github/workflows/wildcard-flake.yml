permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - "flake.nix"
      - "flake.lock"
      - "scripts/zcc/**"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
