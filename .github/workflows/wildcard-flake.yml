permissions: {}
on:
  push:
    branches:
      - wildcard
    paths:
      - ".cargo/zig/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard-flake.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
