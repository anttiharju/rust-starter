name: "Release to crates.io"
description: "Publish the Rust crate to crates.io"
inputs:
  version:
    description: "Version to release"
    required: true
runs:
  using: "composite"
  steps:
    - name: Patch Cargo.toml version
      shell: sh
      env:
        tag: "v${{ inputs.version }}"
      run: |
        version="${tag#v}"
        toml set Cargo.toml package.version "$version" > Cargo.toml.tmp && mv Cargo.toml.tmp Cargo.toml
        cargo update --package "${GITHUB_REPOSITORY#*/}"

    - name: Authenticate
      id: auth
      uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5
    - name: Publish
      shell: sh
      env:
        CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
      run: |
        cargo publish --no-verify --all-features --allow-dirty
